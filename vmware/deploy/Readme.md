# Deploy a VM using the VMWare SDK

Minimal script to deploy a VM

* Create content library
* Create a Windows VM 
* Clone the VM to template in library
* Fill in parameters in the deploy.py script
* Run the deploy.py script
